## 1. 基础设施

> 任务 1.1 记录的是**删除 `StaticVector` 之前**的落地形态（当时移植了 `src/vulkan/utils/StaticVector.h`）；该类已按用户决策删除，最终状态见任务 5.8，`src/vulkan/utils/` 目录一并移除。

- [x] 1.1 等待信号量数组的承载方式：初稿移植上游 `fvkutils::StaticVector<T, 2>` 到 `src/vulkan/utils/StaticVector.h`（并修正其 `Back()` 的越界一位）；后经评估删除，改用 `std::array<T, kMaxWaitSemaphores>` + 共享计数成员（见 5.8）
- [x] 1.2 `src/vulkan/VkDef.h`：新增 `constexpr VkAllocationCallbacks const* kVkAlloc = nullptr;` 与 `constexpr int kMaxCommandBuffers = 3 * 15;`，与既有 `INVALID_VK_INDEX`/`kRequiredVulkanVersionMajor` 聚拢放置；`kFenceTimeBeforeEviction` **不**放此处（已落 `VulkanFencePool.cpp` 匿名命名空间）
- [x] 1.3 `include/Backend/DriverDefine.h`：新增 `enum class FenceStatus : int8_t { Error = -1, ConditionSatisfied = 0, TimeoutExpired = 1 };`，并注释说明三值语义（无需移植上游 `FENCE_WAIT_FOR_EVER`）

## 2. sync 层

- [x] 2.1 创建 `src/vulkan/sync/VulkanCmdFence.h`：`#pragma once`，include `Backend/DriverDefine.h`（取 `FenceStatus`）；声明 `friend class VulkanFencePool` 的 `struct VulkanCmdFence`——构造 `(VkFence, std::function<void(VkFence)> recycleFn = nullptr)`、`~VulkanCmdFence()`、`static std::shared_ptr<VulkanCmdFence> Completed() noexcept`、`MarkSubmitted()`（置 `VK_NOT_READY`）、`RefreshStatus(VkDevice)`、`GetVkFence()`、`GetStatus()`、`Wait(VkDevice, uint64_t timeout, std::chrono::steady_clock::time_point until)`、`Cancel()`、私有 `SetStatus(VkResult)`（置值 + `notify_all`）与 `SwapRecycleFn(...)`；成员 `std::shared_mutex m_lock`/`std::condition_variable_any m_cond`/`bool m_canceled`/`VkResult m_status{VK_INCOMPLETE}`/`VkFence const m_fence`/`std::function<void(VkFence)> m_recycleFn`
- [x] 2.2 创建 `src/vulkan/sync/VulkanCmdFence.cpp`：实现构造、析构（回收回调非空时以 `m_fence` 调用）、`Completed()`（`VK_NULL_HANDLE` + 状态置 `VK_SUCCESS`）、`RefreshStatus`（`vkGetFenceStatus` 为 `VK_SUCCESS` 时置状态）、`Wait`（未提交时持共享锁 `wait_until` 等状态脱离 `VK_INCOMPLETE` 或取消 → 已成功/已取消短路 → 锁外 `vkWaitForFences` 并按 `VK_TIMEOUT`/`VK_SUCCESS`/其它分别返回）、`Cancel`、`SwapRecycleFn`
- [x] 2.3 创建 `src/vulkan/sync/VulkanFencePool.h/.cpp`：构造 `(const VulkanContextPtr&, VkDevice, uint32_t minPoolSize)` 预分配围栏；`AcquireFenceStatus()` 取空闲围栏并返回绑定本池回收回调的 `shared_ptr<VulkanCmdFence>`，同时把 `weak_ptr` 登记进 `m_fenceStatuses`；`Gc()` 先清过期 `weak_ptr`，`++m_currFrame <= kFenceTimeBeforeEviction` 时提前返回，之后在数量大于 `minPoolSize` 的前提下淘汰 `frame + kFenceTimeBeforeEviction < m_currFrame` 者；`Terminate()` 把仍存活者的回收回调换成 `vkDestroyFence` 再销毁池内空闲围栏；私有 `AcquireFence`/`ReleaseFence`（`vkResetFences` + 带帧号回插）/`AllocateFence`（`GetFenceExportFlags()` 非零时链 `VkExportFenceCreateInfo`）/`DestroyFence`；匿名命名空间定义 `constexpr uint32_t kFenceTimeBeforeEviction = 3;`；成员用 `VulkanContextPtr`（另加显式删除拷贝：上游靠引用成员隐式删除，改 `Ptr` 后不再成立）
- [x] 2.4 创建 `src/vulkan/sync/VulkanSemaphore.h/.cpp`：`#pragma once`，include `vulkan/resource/Resource.h`；前向声明并 `DECLARE_SHARE_PTR_CLASS(VulkanSemaphoreManager)`；`struct VulkanSemaphore : public Resource`——构造 `(const VulkanSemaphoreManagerPtr&, VkSemaphore)`、析构调 `m_manager->Recycle(m_semaphore)`、`GetVkSemaphore()`；成员存 `VulkanSemaphoreManagerPtr m_manager` 与 `VkSemaphore m_semaphore`
- [x] 2.5 创建 `src/vulkan/sync/VulkanSemaphoreManager.h/.cpp`：include `vulkan/resource/ResourceManager.h` 与 `vulkan/sync/VulkanSemaphore.h`；构造 `(VkDevice, const ResourceManagerPtr&)` 预分配 `kMaxCommandBuffers` 个 `VkSemaphore`（私有 helper 经 `vkCreateSemaphore` + `kVkAlloc`）；`Acquire()` 优先取池尾、池空则新建，统一经 `m_resourceManager->AllocateAndConstruct<VulkanSemaphore>(this, semaphore)` 返回 `NS_UTILS::SharedPtr<VulkanSemaphore>`；`Recycle(VkSemaphore)` 推回池；`Terminate()` 全销毁并清池；`friend struct VulkanSemaphore`；类继承 `NS_UTILS::Ref` 并在头注释记录「须经 Ptr 堆上持有」的约束

## 3. 资源层类型表接入

- [x] 3.1 `src/vulkan/resource/Resource.h`：新增 `struct VulkanSemaphore;` 前向声明（与 `VulkanBuffer` 聚拢）与 `template <> ResourceType Resource::GetTypeEnum<VulkanSemaphore>() noexcept;` 特化声明
- [x] 3.2 `src/vulkan/resource/Resource.cpp`：定义特化返回 `ResourceType::Semaphore`（**不** include `VulkanSemaphore.h`，沿用 `VulkanBuffer` 的不完整类型写法）
- [x] 3.3 `src/vulkan/resource/ResourceManager.cpp`：include `vulkan/sync/VulkanSemaphore.h`（与既有 `vulkan/stage/VulkanStageBuffer.h` 等 include 聚拢），并在 `destroyWithType` 增加 `case ResourceType::Semaphore: destruct<VulkanSemaphore>(Handle<VulkanSemaphore>(id)); break;`
- [x] 3.4 复核无循环依赖：`Resource.h` 不 include sync 头；`VulkanSemaphore.h` 只依赖 `Resource.h` + 前向声明的 Manager Ptr；`VulkanSemaphoreManager.h` 才 include `ResourceManager.h`

## 4. 平台队列访问器

- [x] 4.1 `include/Backend/platform/VulkanPlatform.h`：新增 3 个 `NODISCARD` 图形队列访问器声明——`GetGraphicsQueueFamilyIndex()`/`GetGraphicsQueueIndex()`/`GetVkGraphicsQueue()`，与既有 `GetVkInstance()`/`GetVkPhysicalDevice()`/`GetVkDevice()` 聚拢
- [x] 4.2 `src/vulkan/platform/VulkanPlatform.cpp`：实现 3 个访问器，转发 `VulkanPlatformPrivate` 的 `m_pGraphicsQueue`/`m_graphicsQueueFamilyIndex`/`m_graphicsQueueIndex`（受保护队列的 3 个访问器已随 5.9 去掉）

## 5. commands 层

> 任务组 5 记录的是**一类一文件拆分前**的落地形态（当时四类同置 `VulkanCommands.h/.cpp`）。拆分为四个同名文件对后：`createCommandBuffer` helper 与 `s_ageCounter` 归 `VulkanCommandBuffer.cpp`，`VulkanGroupMarkers` 实现归 `VulkanGroupMarkers.cpp`（整个文件亦在 `BVK_DEBUG_GROUP_MARKERS` 守卫内），任务组 5 中「同头文件」的表述已由任务 5.7 取代。

- [x] 5.1 `VulkanGroupMarkers`：`using Timestamp = std::chrono::time_point<std::chrono::high_resolution_clock>`；`Push(NS_UTILS::String const&, Timestamp start = {})`（空时间戳取 `now()`）/`Pop()`/`PopBottom()`/`Top()`/`Empty()`；成员 `std::list<std::pair<NS_UTILS::String, Timestamp>> m_markers`；整个类由 `BVK_ENABLED(BVK_DEBUG_GROUP_MARKERS)` 守卫
- [x] 5.2 `VulkanCommandBuffer`：构造 `(const VulkanContextPtr&, VulkanFencePool&, VkDevice, VkQueue, VkCommandPool, const VulkanSemaphoreManagerPtr&)`，拷贝删去；模板 `Acquire(NS_UTILS::SharedPtr<T>)`（`enable_if_t<is_base_of_v<Resource, T>>`，`m_resources.emplace_back(resource.Get())`）；`Reset()`/`Begin()`/`Submit()`/`InsertWait(VkSemaphore, VkPipelineStageFlags)`/`PushMarker`/`PopMarker`/`InsertEvent`/`RefreshStatus(VkDevice)`/`GetStatus()`/`GetFenceStatus()`/`GetVkFence()`/`Buffer()`/`Age()`；成员 `std::vector<NS_UTILS::SharedPtr<Resource>> m_resources`、`std::array<VkSemaphore, kMaxWaitSemaphores> m_waitSemaphores`、`std::array<VkPipelineStageFlags, kMaxWaitSemaphores> m_waitSemaphoreStages` 与共享计数 `m_waitSemaphoreCount`、`std::shared_ptr<VulkanCmdFence> m_fenceStatus`、`NS_UTILS::SharedPtr<VulkanSemaphore> m_submission`、静态 `s_ageCounter`
- [x] 5.3 `VulkanCommandBufferPool`（`using ActiveBuffers = std::bitset<kMaxCommandBuffers>;`、`static constexpr int8_t kInvalid = -1;`、`static_assert(kMaxCommandBuffers < 128);`）：构造 `(const VulkanContextPtr&, VkDevice, VkQueue, uint8_t queueFamilyIndex, const VulkanSemaphoreManagerPtr&)`、析构、`GetRecording()`/`IsRecording()`/`Flush()`/`Gc()`/`Update()`/`Wait()`/`WaitFor(VkSemaphore, VkPipelineStageFlags)`；`BVK_ENABLED(BVK_DEBUG_GROUP_MARKERS)` 下的 `TopMarker()`/`PushMarker`/`PopMarker`/`InsertEvent`；成员含 `std::vector<std::unique_ptr<VulkanCommandBuffer>> m_buffers`、`ActiveBuffers m_submitted`、`int8_t m_recording`、`VulkanFencePool m_fencePool`（值成员，`minPoolSize = kMaxCommandBuffers`）、调试用 `std::unique_ptr<VulkanGroupMarkers> m_groupMarkers`；**不**移植上游未被使用的 `BufferList` typedef
- [x] 5.4 `VulkanCommands`：构造 `(VkDevice, VkQueue, uint32_t queueFamilyIndex, const VulkanContextPtr&, const VulkanSemaphoreManagerPtr&)`；`Get()`/`Flush()`/`Wait()`/`Gc()`/`UpdateFences()`/`AcquireFinishedSignal()`/`GetMostRecentFenceStatus()`/`InjectDependency(VkSemaphore, VkPipelineStageFlags)`/`Terminate()`；`BVK_ENABLED(BVK_DEBUG_GROUP_MARKERS)` 下的 `PushGroupMarker`/`PopGroupMarker`/`InsertEventMarker`/`GetTopGroupMarker`；成员 `m_lastFenceStatus` 初值为 `VulkanCmdFence::Completed()`
- [x] 5.5 实现文件级 helper `createCommandBuffer(VkDevice, VkCommandPool)`（`VK_COMMAND_BUFFER_LEVEL_PRIMARY`、`commandBufferCount = 1`）、`VulkanGroupMarkers`、`VulkanCommandBuffer`（`Reset` 换新围栏状态与提交信号量；`Submit` 先弹尽标记再 `vkEndCommandBuffer`，组装 `VkSubmitInfo` 后 `vkQueueSubmit` 并 `MarkSubmitted`、把等待计数归零）、`VulkanCommandBufferPool`（命令池 flags 含 `RESET_COMMAND_BUFFER`/`TRANSIENT`；`GetRecording` 池满时循环 `Wait` + `Gc`；切换缓冲时按自底向上顺序重建概念标记栈；`std::bitset` 无 `forEachSetBit`，位图遍历改为按下标 `test()`）、`VulkanCommands`（`Flush` 单池提交，注入依赖与 `m_lastSubmit` 链式等待阶段为 `FRAGMENT_SHADER | TRANSFER`，`Terminate` 重置池与最近提交状态；`Wait`/`Gc`/`UpdateFences` 补上游缺失的「池已销毁」早退保护）
- [x] 5.6 复核上游偏离点已按设计落实：`BufferList` typedef 未移植、`sizeof(ActiveBuffers)*8 >= CAPACITY` 断言改为 `kMaxCommandBuffers < 128`、`get`/`flush`/`gc`/`wait`/`update` 全部改为 PascalCase、无 `FVK_SYSTRACE_*`
- [x] 5.7 按用户决策把 `commands/` 拆成**一类一文件**：新增 `VulkanGroupMarkers.h/.cpp`、`VulkanCommandBuffer.h/.cpp`、`VulkanCommandBufferPool.h/.cpp`，`VulkanCommands.h/.cpp` 只保留门面类；各文件按最小依赖收敛 include（`VulkanCommandBuffer.h` 不引入池与门面，`VulkanCommandBufferPool.h` include 缓冲与标记栈，`VulkanCommands.h` include 池与标记栈）；同步修订 `proposal.md`、`design.md` D2（记录布局偏离与理由）、`specs/vulkan-commands/spec.md`（新增「一类一文件」Scenario，并修正 `VulkanCommandBuffer` 构造签名中的 `VulkanSemaphoreManager&` → `const VulkanSemaphoreManagerPtr&`）；全量构建通过、8 个文件 clang-format 零违规、`-DBACKEND_DEBUG_FLAG=3` 下调试路径（含整文件被守卫的 `VulkanGroupMarkers.cpp`）编译通过
- [x] 5.8 按用户决策**删除 `StaticVector`**：确认全仓仅 `VulkanCommandBuffer` 一个使用点、且只用到 `PushBack`/`Clear`/`Size`/`Data`，改用 `std::array<VkSemaphore, kMaxWaitSemaphores>` + `std::array<VkPipelineStageFlags, kMaxWaitSemaphores>` + 共享 `m_waitSemaphoreCount`（`kMaxWaitSemaphores = 2`，`InsertWait` 为唯一写入口并以 `LOG_ASSERT` 拦截越界）；删除 `src/vulkan/utils/StaticVector.h` 与空目录 `src/vulkan/utils/`；同步修订 `proposal.md`（去掉移植项、16 个新增文件）、`design.md`（D2 目录树去掉 utils/、D12 由「修正 off-by-one」改写为「不引入 StaticVector 的理由与替代方案」、风险项改写）、`specs/vulkan-sync/spec.md`（删除「StaticVector 定长容器」需求）、`specs/vulkan-commands/spec.md`（等待数组表述与容器映射改为「StaticVector 不移植」）、`tasks.md` 1.1/5.2/5.6/6.2/6.3；全量构建通过、改动文件 clang-format 零违规
- [x] 5.9 按用户决策**去掉受保护（protected）命令路径**：实测本机 Apple M4 / MoltenVK 1.4.1 上 `protectedMemory = FALSE` 且 0 个队列族带 `VK_QUEUE_PROTECTED_BIT`，该分支在 macOS 上永不可达且会撞 `GetProtected()` 的断言。去掉范围：`VulkanCommandBuffer` 的 `isProtected` 形参与 `m_isProtected`、`Submit()` 的 `VkProtectedSubmitInfo`；`VulkanCommandBufferPool` 的 `isProtected` 形参与 `VK_COMMAND_POOL_CREATE_PROTECTED_BIT`；`VulkanCommands` 的 `protectedQueue`/`protectedQueueFamilyIndex` 形参、`m_protectedPool`、`GetProtected()`，以及 `Flush`/`Wait`/`Gc`/`UpdateFences`/`Terminate` 与四个调试标记接口退化为单池；`VulkanPlatform` 去掉本次为它新增的 3 个受保护队列访问器。**保留不动**：`VulLogicDevice`/`VulQueue`/`VulkanContext` 的受保护内存支持（平台层既有能力）。同步修订 `proposal.md`（新增「不移植 protected 路径」条目、访问器 6→3）、`design.md`（新增 D14 记录依据与代价、D2/D10/Goals 改写、删去原风险项）、`specs/vulkan-commands/spec.md`（门面/依赖链/提交/池构造/平台访问器五处改写，删 3 个受保护 Scenario）、`tasks.md` 4.1/4.2/5.2/5.3/5.4/5.5；全量构建通过、改动文件 clang-format 零违规

## 6. 构建验证与复核

- [x] 6.1 全量构建（`cmake --build build`）：`Backend` 与 `BackendTests` 均通过，新增/改动文件无警告（仅剩 `3rd/Utils` 既有 `nonportable-include-path`）
- [x] 6.2 clang-format 复核：**16 个新增文件均为 0 违规**；`include/Backend/DriverDefine.h` 格式化后 0 违规（仅新增枚举块的列对齐被调整）；`src/vulkan/VkDef.h` 新增行无违规，其余违规为 `34385a8` 放宽列宽至 150 之前遗留的宏块与 include 顺序，与本次无关。另修正了初稿用 `<Utils/...>` 角括号与项目既有 `"Utils/..."` 引号写法不一致、导致 clang-format 分类错位的问题
- [x] 6.3 范围边界 grep：`grep -rn "FVK_\|bluevk\|VulkanProgram\|VulkanSync\|VulkanTimerQuery\|resource_ptr\|FixedCapacityVector\|StaticVector\|utils::CString\|bitset64" src/vulkan/sync src/vulkan/commands src/vulkan/VkDef.h` 无命中
- [x] 6.4 命名与规范复核：公开 API PascalCase、私有成员 `m_camelCase`、常量 `kPascalCase`、无 Filament license/文件头注释、无复述性注释；design D11 的三条生命周期约束已写入 `VulkanSemaphoreManager.h`/`VulkanFencePool.h`/`VulkanSemaphore.h` 的类注释与 `vulkan-sync` spec
- [x] 6.5 记录验证缺口：本变更产物在仓库内无调用方，spec 中运行期 Scenario 均为契约描述，实际验证列为 driver 接线变更的验收项。实测：`UtilsTests` 120 项全通过；`BackendTests` 仍被既有缺口阻塞（`DriverConfig::handleArenaSize` 默认 0 → `HandleAllocatorVK` 的 `FreeList::Init` 断言，`FreeList.cpp:15`），非本次引入。另：`VulkanGroupMarkers` 与两处调试标记接口位于 `BVK_DEBUG_GROUP_MARKERS` 守卫内、默认构建不参与编译，故以 `-DBACKEND_DEBUG_FLAG=3` 单独编译 `VulkanCommands.cpp` 验证该路径，退出码 0
