## 1. 实现 Impl.h

- [x] 1.1 在 `3rd/Utils/include/Utils/` 下创建 `Impl.h`，实现 `utils::Impl<T>` 模板类（声明 + 实现合一）

## 2. 更新 Utils 现有代码

- [x] 2.1 更新 `3rd/Utils/src/Utils.cpp`，将代码包裹在 `namespace utils {}` 中

## 3. 验证

- [x] 3.1 执行 CMake 构建验证编译通过

## 4. Utils 仓库提交

- [x] 4.1 在 Utils 子模块中提交并推送变更
- [x] 4.2 在 Backend 仓库中更新 submodule 引用并提交
